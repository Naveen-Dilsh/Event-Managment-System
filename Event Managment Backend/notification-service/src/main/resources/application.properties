spring.application.name=notification-service
server.port=8087

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/event_management_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:275@ash112}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Email Configuration (Gmail example - use mock for development)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:ashininimna1376@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Service URLs for OpenFeign
booking.service.url=${BOOKING_SERVICE_URL:http://localhost:8083}
event.service.url=${EVENT_SERVICE_URL:http://localhost:8081}
